package passby;

public class Number {
    int myNumber = 0;

    // method syntax
    // access return name (args) {implementation}
    // access = public, private, protected, or left unspecified for default
    // return = void for not returning anything or datatype (e.g. int, String, double)
    public void AddToNumber(int x) {
        myNumber += x;
    }
}
